# Generated runtime configuration

Run `uv run agent-trust-generate` from the repository root to create:

- `policy.json` — signed authorization policy
- `keyring.json` — public verification keys used by the relay
- `signing-key.pem` — private signing key; keep secret and do not give it to the relay

Only this README is committed. Generated configuration and signing material are
ignored by Git.

